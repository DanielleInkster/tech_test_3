class Total{
constructor() {
  this.tax = 0
  this.pre_tax_total = 0
  this.amount_owed= 0
  }

  item_price(item){
    let menu = new Menu
      return menu.options[item]
    }

  item_total(num,item){
    return num * this.item_price(item)
  }
}